package franz.functionality

import franz.BaseFranzTest

class RichPublisherTest extends BaseFranzTest {

  "RichPublisher" should {
    "be able to publish avro, json, bytes, strings or longs base on the types" in {
      println("see")
    }
  }
}
